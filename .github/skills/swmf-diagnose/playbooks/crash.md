# Playbook: Runtime Crash / Stop

1. Collect run context and locate first failing log line.
2. Extract stacktrace/rank evidence.
3. Correlate with component map and scheduler layout.
4. Stage mechanism candidates with confidence and required checks.
5. If source edits are considered, require invariant context first.
